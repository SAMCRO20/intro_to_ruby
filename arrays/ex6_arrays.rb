# The error occurs when calling the names array with 'margaret' as the index, which should be an integer not a string.
# If you would like to change 'margaret' to 'jody', then use index 3 for the 4th element.

names = ['bob', 'joe', 'susan', 'margaret']
names[3] = 'jody'

puts names # to check my work